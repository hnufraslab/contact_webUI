## 点云处理工具更新说明

### 更新内容

1. **修复了点云格式解析问题**
   - 自动读取字段偏移量，支持不同格式的点云数据
   - 解决了 intensity 字段偏移量不匹配的问题

2. **修复了 Web 端交互问题**
   - 订阅正确的 topic：`/planning/crop_box`、`/planning/crop_box_size`、`/planning/crop_reset`
   - 添加了三个独立的回调函数处理裁剪框、尺寸和重置命令

3. **改进了日志输出**
   - 首次接收点云时显示确认信息
   - 详细的裁剪框信息输出
   - 清晰的裁剪结果统计

### 使用方法

1. **重启节点**：
```bash
# 停止旧节点（如果在运行）
rosnode kill /pointcloud_processor

# 启动新节点
./start_processor.sh
```

2. **在 Web UI 中测试**：
   - 打开浏览器访问 http://localhost:8000
   - 创建 ROI 裁剪框
   - 点击"发送裁剪框到上位机"按钮
   - 观察点云变化和终端日志

3. **查看日志**：
   - 节点启动时会显示配置信息
   - 接收到裁剪框时会显示位置、姿态、尺寸
   - 裁剪完成后会显示点数变化

### 预期输出示例

```
============================================================
点云处理工具启动
============================================================
输入Topic: /cloud_registered
输出Topic: /cloud_in
裁剪框Topic: /planning/crop_box
裁剪框尺寸Topic: /planning/crop_box_size
裁剪重置Topic: /planning/crop_reset
最大点数: 500000
发布频率: 5.0 Hz
============================================================
等待点云数据...
✓ 成功接收到点云数据，开始处理...
============================================================
收到裁剪框位姿消息
位置: [1.23, 2.45, 3.67]
四元数: [0.000, 0.000, 0.000, 1.000]
收到裁剪框尺寸消息
尺寸: [2.00, 2.00, 2.00]
✓ 启用裁剪模式
✓ 点云裁剪完成: 5395 -> 1234 (保留: 22.87%)
============================================================
```

### 故障排查

如果裁剪不生效：
1. 检查 Web UI 是否连接到 ROS（rosbridge）
2. 检查是否点击了"发送裁剪框到上位机"按钮
3. 查看终端是否有错误信息
4. 使用 `rostopic echo /planning/crop_box` 检查消息是否发布

如果点云不显示：
1. 检查 `/cloud_registered` 是否有数据：`rostopic hz /cloud_registered`
2. 检查 `/cloud_in` 是否有输出：`rostopic hz /cloud_in`
3. 查看节点日志是否有错误
